import styled from "styled-components";

const Main = styled.main`
  .adeola {
    img {
      width: 100%;
    }
    /* p {
      position: absolute;
      left: 7%;
      bottom: 20%;
      color: #fff;
      font-size: 1.8rem;
      font-weight: var(--work-sans-600);
      line-height: 38px;
    } */
  }
`;

export default Main;
